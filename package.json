{
  "name": "codex-bridge",
  "version": "1.0.0",
  "type": "module",
  "description": "Local proxy that routes OpenAI Responses API requests through your Codex/ChatGPT subscription with auto token refresh",
  "scripts": {
    "start": "node bridge.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
